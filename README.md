This project simulates sampling distributions by repeatedly drawing random samples from a dataset and computing their means. It visualizes the distribution of sample means using Plotly and compares it to the population mean.
Built to practice statistical concepts such as random sampling, sampling distributions, and data visualization using Python.
